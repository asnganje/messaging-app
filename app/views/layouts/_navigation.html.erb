<nav class="container mx-auto bg-white shadow-md px-5 rounded py-3 mt-4 flex items-center justify-between">
  <div class="flex items-center space-x-4">
    <p class="text-2xl font-bold text-green-500">
      Logo
    </p>
    <% if signed_in?%>
      <span class="text-gray-700 font-medium">👋 <%= current_user.username%><strong>
    <%end%>
  </div>
  <div class="flex items-center space-x-4">
    <% if signed_in?%>
      <%= link_to "Logout", destroy_user_session_path,
      method: :delete,
      class:"btn bg-orange-400 px-4 py-2 rounded-md text-white transition duration-200" %>
    <%else%>
      <%= link_to "Login", new_user_session_path, class:"btn bg-orange-400 text-white hover:bg-orange-600 px-4 py-2 rounded-md transition duration-200" %>
      <%= link_to "Sign up", new_user_registration_path, class:"btn bg-gray-400 px-4 py-2 text-white hover:bg-gray-600 rounded-md transition duration-200" %>
    <%end%>
  </div>
</nav>